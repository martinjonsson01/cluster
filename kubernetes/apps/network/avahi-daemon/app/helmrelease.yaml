---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: avahi-daemon
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      avahi-daemon:
        type: daemonset
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: flungo/avahi
              tag: latest@sha256:b41417c00fd8557e2832b88f815bfda84d69b37ad448e688d27a925fbe61865f
            env:
              REFLECTOR_ENABLE_REFLECTOR: "yes"
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 128Mi

    persistence:
      dbus:
        enabled: true
        type: hostPath
        hostPath: /run/dbus/system_bus_socket
        hostPathType: Socket
